#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int n;
    scanf("%d",&n);
    if(a+b>c || b+c>a || c+a>b)
    printf("Valid");
    else
    printf("Not Valid");
    return 0;
}